project_id = "gnumzynfewmurvykopxq"

[functions.identify-flower]
# JWT verification is manual for current Supabase gateway compatibility: the
# handler allows OPTIONS, requires Bearer, and validates it with auth.getUser.
verify_jwt = false
